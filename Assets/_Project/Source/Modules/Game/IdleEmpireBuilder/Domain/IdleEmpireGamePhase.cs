namespace IdleEmpireBuilder.Domain
{
    /// <summary>High-level session phase for the idle sample (extendable for menus / soft pause).</summary>
    public enum IdleEmpireGamePhase
    {
        Playing = 0
    }
}
